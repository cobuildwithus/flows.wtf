{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Generate contract ABIs for web & indexer",
  "scripts": {
    "generate": "wagmi generate",
    "postgenerate": "cp src/generated.ts ../web/lib/abis.ts && cp src/generated.ts ../indexer/abis.ts && cp addresses.ts ../web/addresses.ts && cp addresses.ts ../indexer/addresses.ts"
  },
  "dependencies": {
    "@types/node": "^22.5.5",
    "typescript": "^5.6.2",
    "@wagmi/cli": "^2.3.1"
  }
}
